import React from 'react';
import {View} from 'react-native';

const Line = () => {
  return (
    <View
      style={{
        borderColor: 'black',
        borderWidth: 1,
        marginBottom: 14,
      }}></View>
  );
};

export default Line;
